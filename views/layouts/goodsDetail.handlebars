


{{#if goods}}
        <ul>
        <li>{{goods.description}}</li>
        </ul>


 <form action="/buy" method="post">

<input type="text" name="buygoodsid" value="{{goods._id}}" />
<input type="text" name="buygoodsname" value="{{goods.goods}}" />
 <input type="submit" value="buy it now" />

 </form>


 <form action="/addshoppingcart" method="post">

<input type="text" name="cartgoodsid" value="{{goods._id}}" />
<input type="text" name="cartgoodsname" value="{{goods.goods}}" />
<input type="text" name="cartgoodsprice" value="{{goods.goods}}" />
 <input type="submit" value="add to cart" />

 </form>


 <form action="/goodscomment" method="post">

    <div>
        <label>comment:</label>
        <input type="text"  name="comment"/>
    </div>

   <div>

   <!--  {{#if goods}}
       <label id="id">{{goods._id}}</label>
    {{/if}} -->
   	 
   </div>

    <div>
        <input type="submit" value="submit"/>
    </div>

</form>
 {{/if}}
{{message}}